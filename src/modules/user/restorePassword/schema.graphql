"""
# import Error from '../../shared.graphql'

type Query {
  dummy: String
}
"""

type Mutation {
  sendForgotPasswordEmail(email: String!): Boolean
  restorePasswordChange(password: String!, key: String!): [Error!]
}
